# docker-compose.db.yml — PROD Environment
# Project : SaaS Blueprint V1
# Variant : pgvector (postgres 18 + vector extension, replaces Qdrant)
# Generated by gus-factory / create_project.py
# ──────────────────────────────────────────────────────────────────
# Copied to the project root by scripts/switch_postgres_variant.py.
# To switch variants: `python scripts/switch_postgres_variant.py pgvector`

name: gus-saas-blueprint-v1

services:

  # ── PostgreSQL PROD ─────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg18-trixie
    container_name: saas-blueprint-v1-postgres
    command: >
      postgres
      --hba-file=/etc/saas-blueprint-config/pg_hba.conf
      -c wal_level=replica
      -c max_wal_senders=3
      -c wal_keep_size=128
      -c hot_standby=on
    environment:
      POSTGRES_DB:       saas_blueprint_v1
      POSTGRES_USER:     saas_blueprint_v1
      POSTGRES_PASSWORD: saas_blueprint_v1
      PGDATA:            /var/lib/postgresql/data
      TZ:                America/Sao_Paulo
      PGTZ:              America/Sao_Paulo
    ports:
      - "5444:5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
      - ./infra/postgres/primary:/docker-entrypoint-initdb.d
      - ./infra/postgres/primary/postgresql.conf:/etc/saas-blueprint-config/postgresql.conf:ro
      - ./infra/postgres/primary/pg_hba.conf:/etc/saas-blueprint-config/pg_hba.conf:ro
    networks:
      - saas-blueprint-v1-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U saas_blueprint_v1 -d saas_blueprint_v1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # @IF replica
  # ── PostgreSQL PROD Replica (DB_ENABLE_REPLICA=true) ───────────
  postgres-replica:
    image: pgvector/pgvector:pg18-trixie
    container_name: saas-blueprint-v1-postgres-replica
    entrypoint: ["/replica/entrypoint.sh"]
    environment:
      POSTGRES_DB:       saas_blueprint_v1
      POSTGRES_USER:     saas_blueprint_v1
      POSTGRES_PASSWORD: saas_blueprint_v1
      PGDATA:                        /var/lib/postgresql/data
      POSTGRES_PRIMARY_HOST:         postgres
      POSTGRES_PRIMARY_PORT:         5432
      POSTGRES_REPLICATION_USER:     replicator
      POSTGRES_REPLICATION_PASSWORD: replicator_password
      TZ:                            America/Sao_Paulo
      PGTZ:                          America/Sao_Paulo
    ports:
      - "5445:5432"
    volumes:
      - postgres_data_prod_replica:/var/lib/postgresql/data
      - ./infra/postgres/replica/entrypoint.sh:/replica/entrypoint.sh
    networks:
      - saas-blueprint-v1-network
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U saas_blueprint_v1 -d saas_blueprint_v1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s
  # @ENDIF replica

  # @IF redis
  # ── Redis PROD (included if CACHE_LAYER=Redis) ──────────────────
  redis:
    image: redis:7-alpine
    container_name: saas-blueprint-v1-redis
    command: redis-server --appendonly yes
    ports:
      - "6385:6379"
    volumes:
      - redis_data_prod:/data
    networks:
      - saas-blueprint-v1-network
    restart: unless-stopped
  # @ENDIF redis

  # @IF etl
  # ── RabbitMQ PROD (included if QUEUE_LAYER=RabbitMQ) ───────────
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: saas-blueprint-v1-rabbitmq
    ports:
      - "5675:5672"
      - "15675:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
      RABBITMQ_DEFAULT_VHOST: /
    volumes:
      - rabbitmq_data_prod:/var/lib/rabbitmq
    networks:
      - saas-blueprint-v1-network
    restart: unless-stopped
  # @ENDIF etl

  # @IF qdrant
  # ── Qdrant PROD (included if EMBEDDING_DB=Qdrant) ──────────────
  qdrant:
    image: qdrant/qdrant:latest
    container_name: saas-blueprint-v1-qdrant
    ports:
      - "6343:6333"
      - "6344:6334"
    volumes:
      - qdrant_data_prod:/qdrant/storage
    networks:
      - saas-blueprint-v1-network
    restart: unless-stopped
  # @ENDIF qdrant

volumes:
  postgres_data_prod:
    name: saas_blueprint_v1_postgres_data_prod
  # @IF replica
  postgres_data_prod_replica:
    name: saas_blueprint_v1_postgres_data_prod_replica
  # @ENDIF replica
  # @IF redis
  redis_data_prod:
    name: saas_blueprint_v1_redis_data_prod
  # @ENDIF redis
  # @IF etl
  rabbitmq_data_prod:
    name: saas_blueprint_v1_rabbitmq_data_prod
  # @ENDIF etl
  # @IF qdrant
  qdrant_data_prod:
    name: saas_blueprint_v1_qdrant_data_prod
  # @ENDIF qdrant

networks:
  saas-blueprint-v1-network:
    driver: bridge
    name: saas-blueprint-v1-network
